fastapi>=0.100.0
uvicorn[standard]>=0.20.0
httpx>=0.24.0
python-dotenv>=1.0.0

# MDX dictionary reader (replaces mdict-query which is not on PyPI)
mdict-utils>=1.3.14
readmdict>=0.0.4
anki==25.9.2

# Required if LDOCE5 MDX uses LZO compression (MDict v1.x format)
python-lzo>=1.14

# Dev / test dependencies
pytest>=7.0
pytest-asyncio>=0.21.0
